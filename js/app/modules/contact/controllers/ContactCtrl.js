'use strict';

/**
 * 
 * @param {type} $scope scope
 */
function ContactCtrl($scope, $modal, $filter, UtilsFactory, MainFactory) {

    $scope.title = 'contact';
    $scope.isAdmin = false;
    // existing projects
   

    $scope.init = function() {
        // do some stuff
    };

   

}
